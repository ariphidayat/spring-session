<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:sec="http://www.springframework.org/schema/security"
       xsi:schemaLocation="http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security.xsd
       http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">

    <sec:http auto-config="true">
        <sec:intercept-url pattern="/login" access="permitAll"/>
        <sec:intercept-url pattern="/hello/new" access="hasRole('ROLE_ADMIN')" />
        <sec:intercept-url pattern="/**" access="isAuthenticated()" />

        <sec:form-login
                login-page="/login"
                authentication-failure-url="/login?error"
                authentication-success-handler-ref="authSuccessHandler"
                username-parameter="username"
                password-parameter="password"
        />

        <sec:logout logout-success-url="/login?logout"/>

        <sec:csrf/>
    </sec:http>

    <bean id="authSuccessHandler" class="org.arip.SavedRequestAwareAuthSuccessHandler"/>

    <sec:user-service>
        <sec:user name="arip" password="hidayat" authorities="ROLE_USER" />
        <sec:user name="alisiana" password="ulfah" authorities="ROLE_USER" />
    </sec:user-service>

</beans>